{
  "name": "@monopoly/server",
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build && node test/unit.mjs && node test/sim.mjs && node test/regression.mjs",
    "test:unit": "npm --prefix .. run build --workspace=@monopoly/shared && npm run build && node test/unit.mjs",
    "test:sim": "npm --prefix .. run build --workspace=@monopoly/shared && npm run build && node test/sim.mjs"
  },
  "dependencies": {
    "@monopoly/shared": "*",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.13.0",
    "redis": "^4.7.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.4",
    "@types/pg": "^8.11.0",
    "socket.io-client": "^4.8.1",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
